/**
 * Contextual Tooltip Demo Page
 * 
 * Demonstrates the contextual tooltip system for first-time feature guidance.
 */

import { ContextualTooltipDemo } from "@/components/ui/contextual-tooltip-examples";

export default function ContextualTooltipDemoPage() {
    return <ContextualTooltipDemo />;
}
